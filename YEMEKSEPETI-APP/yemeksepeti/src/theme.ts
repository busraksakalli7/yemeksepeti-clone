import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#e30c1b',  // Yemeksepeti kırmızısı
    },
    secondary: {
      main: '#ffffff',  // Beyaz
    },
  },
});

export default theme;
